# Copy to .env for local overrides
POSTGRES_PASSWORD=sdf
# Host-side Kafka bootstrap uses redpanda's external listener (port 19092).
# In-network services (ingest/api/bridge) use redpanda:9092 set in docker-compose.
KAFKA_BOOTSTRAP=localhost:19092
MQTT_URL=tcp://localhost:1883
